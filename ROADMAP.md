# LiteBoard Roadmap - UI Baseline + MVP First

> 本路线图用于约束接下来的实现顺序。当前阶段不追求一次性做成完整生产级平台，而是先做一轮有限范围的 UI 基线升级，让产品具备清晰、专业、可继续投入的外壳；随后马上回到核心闭环：数据接入 -> 图表配置 -> 画布排版 -> 保存恢复 -> 导出运行。

---

## 1. 目标判断

LiteBoard / SchemaV 的长期目标是合理的：它要做一个低代码大屏可视化编辑器，允许用户接入数据、拖拽排版、配置图表，并把最终大屏序列化为 Schema 或导出为可运行的 Vue 单文件组件。

但这个目标必须拆成两层：

- **MVP 可交付目标**：稳定完成数据接入、图表配置、画布排版、保存、恢复、导出。
- **生产级长期目标**：多人使用、组件生态、权限、版本管理、数据安全、协作能力、完整测试与可靠发布。

当前项目已经具备功能原型基础，但还没有达到生产级。由于现有 UI 会干扰对功能体验的判断，后续开发先做一轮克制的 UI 基线升级，再稳定 MVP 核心闭环，最后逐步扩展组件和生产化能力。

---

## 2. 当前状态与最高优先级

### 2026-06-05 更新

已完成两块基础工作：

- **任务大厅 UI 基线**：任务大厅已按工作台式结构重构，形成顶部导航、筛选栏、项目网格和右侧更新/快捷入口。
- **公共数据池 v1**：已按“一个大屏一个 `globalData`”打通首版核心链路。探针可以多次导入多个数据源，用户通过 JS 将接口返回值筛选为字段数组对象；导入前会校验格式，并在同名字段覆盖前提示和确认。图表绑定、保存恢复和导出都按同一套字段数组契约消费 `globalData`。
- **仪表盘产品化闭环**：LiteBoard v1 定位收敛为轻量仪表盘设计器。当前不拆独立数据源表或数据集表，仍以 `globalData` 承载当前仪表盘数据；图表级 `customDataCode` 作为正式能力，运行时与导出必须一致支持。

当前最高优先级从“先统一数据模型”推进为：**继续稳定编辑器 MVP 闭环，并补齐编辑器可用性**。

下一步建议：

- 按 `docs/editor-slim-workbench-requirements.md` 做一轮 DataEase 风格的编辑器瘦身重构：顶部组件栏、中央画布、右侧属性/字段面板，移除左侧常驻大面板。
- 补当前仪表盘数据集的轻量说明和异常提示。
- 补编辑器核心操作：复制、删除、置顶/置底、保存状态提示。
- 增加普通表格、指标卡、文本等 MVP 常用组件。
- 为探针和导出补自动化测试，避免数据契约回退。

公共数据池 v1 细节见：`docs/public-data-pool-v1.md`。

---

## 2. 原始优先级判断

下一步优先做：**有限范围 UI 基线升级**。

这轮 UI 升级只做产品外壳和基础规则，不做大规模业务逻辑重构。目标是让任务大厅和编辑器主界面看起来像一个可信赖的工作台，从而方便后续判断功能问题。

UI 方向：

- **编辑器**：采用方案 1 + 方案 3 的结合，参考 `docs/ui-concepts/editor-light-workbench.png` 与 `docs/ui-concepts/editor-clean-light.png`。保留当前信息架构，即顶部工具栏、左侧数据/组件面板、中央画布、右侧配置面板；优化间距、色彩、按钮、面板层级、画布网格、空状态和配置面板。
- **任务大厅**：参考方案 4，即 `docs/ui-concepts/task-hub-reference.png`。优化顶部栏、搜索、新建入口、项目卡片、状态标签和项目列表层级。
- **设计规则**：建立基础设计变量，包括颜色、边框、阴影、字体大小、面板宽度、按钮风格和状态色。

约束：

- 不重写业务逻辑。
- 不改数据模型、保存逻辑、导出逻辑，除非 UI 结构调整必须轻微触碰。
- 不追求完整设计系统，只形成后续功能开发能沿用的 UI 基线。

UI 基线完成后，立即进入第二优先级：**统一数据模型，并打通完整黄金路径**。

现在最大的结构性风险是 `globalData` 的消费契约。文档希望图表绑定 `globalData` 顶层字段，但实际数据可能保存为：

```json
{
  "data": [
    { "month": "1月", "sales": 150, "profit": 80, "target": 200 }
  ]
}
```

如果字段推导只读取顶层 key，就只能看到 `data`，而不是 `month`、`sales`、`profit`、`target`。这会直接影响图表绑定、保存恢复和导出代码。

因此，UI 基线之后不要急着增加更多组件；要先把数据到图表、保存、恢复、导出的链路做硬。

---

## 3. 阶段计划

### 阶段 0：UI 基线升级

目标：让项目具备清爽、专业、可持续开发的产品外壳。

验收标准：

- 任务大厅改成更成熟的项目管理入口。
- 编辑器保留现有结构，但视觉上接近专业低代码工作台。
- 左侧面板、中央画布、右侧配置面板层级清晰。
- 按钮、输入框、标签、空状态、卡片、面板统一风格。
- 建立可复用的基础 CSS 变量或样式约定。
- 不破坏现有首页、编辑页、保存、导出、探针入口。

优先修复项：

- 任务大厅顶部栏、搜索区、项目卡片、状态标签。
- 编辑器顶部工具栏、左侧面板、画布区域、右侧配置面板。
- 空画布、已挂载数据、未选中组件等关键状态。
- 全局颜色、间距、边框、阴影和字体层级。

### 阶段 1：稳定核心闭环

目标：用户能从零创建一个大屏，并完整保存、恢复、导出。

验收标准：

- 可以新建任务。
- 可以通过探针拉取 mock 数据。
- 系统能自动识别字段，例如 `month`、`sales`、`profit`、`target`。
- 可以添加柱状图或折线图。
- 绑定 X/Y 字段后图表正确显示。
- 拖拽、缩放组件后可以保存。
- 刷新页面后恢复结果一致。
- 导出 Vue 文件后，导出文件能运行并显示同样图表。

优先修复项：

- 明确 `globalData` 数据契约。
- 建立统一字段推导逻辑，兼容数组型数据和对象型数据。
- 让图表绑定逻辑与字段推导逻辑一致。
- 让保存、加载、导出的 Schema 消费方式一致。
- 让 `codeGenerator` 与运行时渲染逻辑尽量同构。

### 阶段 2：编辑器可用性

目标：让编辑器从实验页面变成可连续操作的工具。

重点能力：

- 撤销 / 重做。
- 自动保存或保存状态提示。
- 画布缩放、居中、适配屏幕。
- 组件选中、删除、复制、对齐。
- 更清晰的空状态、错误提示和不可用状态。
- 配置面板对常见数据形态更容错。

### 阶段 3：组件体系

目标：从图表 Demo 变成真正的大屏编辑器。

优先组件：

- 文本组件。
- 指标卡组件。
- 图片组件。
- 表格组件。
- 柱状图、折线图、饼图。
- 容器 / 分组组件。
- 基础装饰组件。

每个组件都必须支持：

- 编辑器中渲染。
- 保存到 Schema。
- 从 Schema 恢复。
- 导出到 Vue SFC。

### 阶段 4：生产化

目标：真实用户可以长期使用。

重点能力：

- 后端存储从本地 JSON 迁移到数据库。
- Schema 版本迁移机制。
- API 参数校验和错误结构统一。
- 探针安全限制，包括 URL 白名单、协议限制、超时、内网访问限制。
- 单元测试、端到端测试、导出产物测试。
- 权限、项目归属、版本历史。

---

## 4. 建议的四周节奏

### 第 1 周：UI 基线升级

- 任务大厅参考方案 4 做结构和视觉升级。
- 编辑器参考方案 1 + 方案 3 做浅色专业工作台升级。
- UI 参考图统一保存在 `docs/ui-concepts/`，后续样式升级必须先对照这些参考图。
- 建立基础设计变量。
- 保证现有核心入口和构建不被破坏。

### 第 2 周：数据契约与字段识别

- 定义 `globalData` 标准形态。
- 实现统一字段推导工具。
- 修复 mock 数据导入后只能看到 `data` 顶层字段的问题。
- 让柱状图 / 折线图能直接绑定数组对象中的字段。

### 第 3 周：保存、恢复、导出同构

- 保存后刷新页面，确保组件、数据、画布配置完全恢复。
- 对齐运行时渲染逻辑和 `codeGenerator` 导出逻辑。
- 导出的 `.vue` 文件应能复现编辑器中的图表。

### 第 4 周：交互稳定性与基础测试

- 补核心交互：添加、删除、拖拽、缩放、保存提示。
- 增加关键工具函数测试。
- 增加黄金路径端到端验证。
- 形成可演示 MVP。

---

## 5. 开发原则

- 先做一轮有限 UI 基线升级，再做核心闭环，最后扩组件。
- 先统一 Schema 契约，再写更多 UI。
- 运行时渲染和导出代码必须尽量共享同一套数据理解。
- 每个新增能力都要考虑保存、恢复、导出三件事。
- 本地 JSON 存储只作为当前阶段方案，不当作生产化终点。
- UI 升级不能变成无限美化；只做能支撑后续功能开发的结构、层级和基础视觉规则。
